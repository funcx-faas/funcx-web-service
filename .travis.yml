matrix:
      include:
        - python: 3.7
          dist: xenial
          sudo: true


before_install:
  - sudo apt-get update -q
  - sudo apt-get install redis
install:
  - cd forwarder
  - pip install .

script:
  - pwd
  - flake8 forwarder
